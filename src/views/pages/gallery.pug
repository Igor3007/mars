extends ../layouts/index


block variables
  include ../../data/data.pug
  - const title = "Mars"


block content

  include ../../blocks/components/breadcrumb/breadcrumb.pug
  include ../../blocks/modules/page-gallery/page-gallery.pug

  .container
    .page 
     .page__breadcrumbs
      +breadcrumb([
        'Главная',
        'Галерея'
      ])
     .page__title
        h1 Галерея
     .page__content
        +page-gallery()

block scripts
  script(src="/js/vendor.js") 
  script(src="/js/common.js") 
 
