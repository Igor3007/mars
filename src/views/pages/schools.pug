extends ../layouts/index


block variables
  include ../../data/data.pug
  - const title = "Mars"


block content

  include ../../blocks/components/breadcrumb/breadcrumb.pug
  include ../../blocks/modules/page-schools/page-schools.pug

  .container
    .page 
     .page__breadcrumbs
      +breadcrumb([
        'Главная',
        'Наши школы'
      ])
     .page__title
        h1 Наши школы
     .page__content
        +page-schools()

block scripts
  script(src="/js/vendor.js") 
  script(src="/js/common.js") 
 
